'use strict'

const colors = require('./colors')
const config = require('./config')
const login = require('./login')
const switchy = require('./switch')

module.exports = {
  colors,
  config,
  login,
  switchy
}
